# Development

- Use `npm run dev:backend` and `npm run dev:frontend` in parallel.
- Linting: add ESLint/Prettier as desired (not included by default).
- Testing: add Vitest/Jest for unit/API tests. Suggested folders `backend/tests` and `frontend/__tests__`.
- Environment: keep `.env` files local; use `.env.example` templates.
